services:
  bot:
    build: .
    container_name: voicetotext-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Europe/Moscow
